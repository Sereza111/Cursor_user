# 🔄 CLINE Local Token Rotator

Локальный скрипт для автоматической ротации токенов CLINE в VS Code.

## 📖 Как это работает

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   [Твой ПК с VS Code]                    [Сервер 109.172.37.69]    │
│                                                                     │
│   ┌─────────────────┐                    ┌─────────────────┐       │
│   │                 │  1. Запрос токена  │                 │       │
│   │  Local Rotator  │ -----------------> │  Панель регист. │       │
│   │                 │                    │                 │       │
│   │                 │  2. Получает токен │   База данных   │       │
│   │                 │ <----------------- │   с токенами    │       │
│   └────────┬────────┘                    └─────────────────┘       │
│            │                                                        │
│            │ 3. Устанавливает                                       │
│            │    токен в VS Code                                     │
│            ▼                                                        │
│   ┌─────────────────┐                                               │
│   │                 │                                               │
│   │  VS Code CLINE  │ <-- Использует новый токен                   │
│   │                 │                                               │
│   └─────────────────┘                                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 🚀 Установка

### 1. Скопируй папку `local-rotator` на свой ПК

```bash
# Или клонируй репозиторий и используй только эту папку
git clone https://github.com/Sereza111/Cursor_user.git
cd Cursor_user/local-rotator
```

### 2. Установи зависимости

```bash
npm install
```

> **Важно:** `keytar` требует компиляции. На Windows нужен:
> - Python 3
> - Visual Studio Build Tools (или полная Visual Studio)
> 
> Если возникают ошибки, установи: `npm install --global windows-build-tools`

### 3. Создай файл конфигурации

```bash
# Скопируй пример
copy .env.example .env

# Отредактируй .env
```

### 4. Настрой .env

```env
# URL твоего сервера с панелью
SERVER_URL=http://109.172.37.69:3000

# API ключ (должен совпадать с TOKEN_API_KEY на сервере!)
API_KEY=твой-секретный-ключ

# Минимальный баланс для замены
MIN_BALANCE=0.10
```

## 📋 Настройка сервера

На сервере в файле `.env` добавь:

```env
# Секретный ключ для API токенов (придумай сложный!)
TOKEN_API_KEY=твой-секретный-ключ-такой-же-как-на-пк
```

## 🎮 Использование

### Разовая проверка и замена

```bash
npm start
```

Скрипт:
1. Проверит текущий токен в VS Code
2. Запросит баланс через CLINE API
3. Если баланс низкий - получит новый токен с сервера
4. Установит новый токен в VS Code

### Только проверка баланса

```bash
npm run check
```

### Автоматическая ротация (daemon)

```bash
npm run daemon
```

Daemon будет проверять баланс каждые 5 минут и автоматически менять токен.

## ⚙️ Параметры .env

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `SERVER_URL` | URL сервера с панелью | `http://localhost:3000` |
| `API_KEY` | Ключ авторизации | - |
| `MIN_BALANCE` | Мин. баланс для замены | `0.10` |
| `CHECK_INTERVAL` | Интервал проверки (сек) | `300` (5 мин) |
| `VERBOSE` | Детальные логи | `false` |

## 🔧 Как добавить в автозагрузку Windows

### Вариант 1: Task Scheduler

1. Открой Task Scheduler (taskschd.msc)
2. Create Basic Task
3. Trigger: When I log on
4. Action: Start a program
   - Program: `node`
   - Arguments: `daemon.js`
   - Start in: `C:\путь\к\local-rotator`

### Вариант 2: Создать .bat файл

```batch
@echo off
cd /d C:\путь\к\local-rotator
node daemon.js
```

Сохрани как `start-rotator.bat` и добавь ярлык в:
`C:\Users\ИМЯ\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup`

## ❓ FAQ

### Токен не меняется?

1. Убедись что VS Code закрыт при первой установке токена
2. После смены токена перезапусти VS Code
3. Проверь что `keytar` установлен корректно

### Ошибка подключения к серверу?

1. Проверь что сервер запущен и доступен
2. Проверь правильность `SERVER_URL` и `API_KEY`
3. Попробуй открыть URL в браузере

### "Нет доступных токенов"?

На сервере закончились токены. Зарегистрируй новые аккаунты CLINE через панель.

## 📝 Логика работы

```
1. Получить текущий токен из VS Code (keytar)
   ↓
2. Проверить баланс через CLINE API
   ↓
3. Баланс < MIN_BALANCE?
   │
   ├─ Да → Запросить новый токен с сервера
   │        → Установить в VS Code
   │        → Уведомить о перезапуске
   │
   └─ Нет → Ничего не делать
```

## 🛡️ Безопасность

- API ключ передаётся в заголовке `X-API-Key`
- Токены хранятся в Windows Credential Manager
- Никакие данные не логируются на диск
